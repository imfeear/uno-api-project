info:
  name: Get Player by ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/players/{{player_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
