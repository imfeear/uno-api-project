info:
  name: Delete Player
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/players/{{player_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
