info:
  name: Get Score by ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/scores/{{score_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
