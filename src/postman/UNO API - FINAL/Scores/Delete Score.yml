info:
  name: Delete Score
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/scores/{{score_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
