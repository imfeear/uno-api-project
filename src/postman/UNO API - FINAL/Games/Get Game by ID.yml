info:
  name: Get Game by ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/games/{{game_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
