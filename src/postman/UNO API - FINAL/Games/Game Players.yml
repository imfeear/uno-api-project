info:
  name: Game Players
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/games/players"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "game_id": "{{game_id}}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "Observação: no código atual esta rota pode falhar por causa da ordem das rotas em gameRoutes.js."
