info:
  name: Delete Card
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/cards/{{card_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
